info:
  name: Book by isbn
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/books/:isbn"
  params:
    - name: isbn
      value: 978-0735211292
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
